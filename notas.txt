gitbash es una consola para manipular el computador

GIT
 
git init inicia un repositorio

git add -A, para enlistar los elementos que quiero guardar

git commit -m "mensaje de guardado", para indicarle a git que guardar

